# Codex 新对话启动提示词

我正在继续一个已经开发了一段时间的项目。

我已经上传 `CODEX_PROJECT_MEMORY.md`。请先完整阅读该文件，并将其视为本项目的长期记忆、协作规则和上下文来源。

请根据该文件恢复我之前的使用习惯，包括：

- 默认使用中文，表达直接、清楚、可执行；
- 修改代码前先检查规则、Git 状态、需求文档、现有代码和完整调用链；
- 只增量修改当前任务相关内容，不重写或顺手修改无关模块；
- 遵守 GitHub 分支、Commit、Push 和 Draft Pull Request 流程；
- 数据库变更必须有迁移、备份、验证和回滚方案；
- 测试、构建、静态检查和部署状态必须依据真实命令输出；
- 不泄露密码、Token、私钥、AccessKey、数据库密码或生产 `.env`；
- 默认不直接修改 `main`、不自动合并 PR、不直接部署生产；
- 完成并推送后，提供一整段可复制到阿里云黑框执行的更新命令和安全回滚方式。

不要让我重新解释 `CODEX_PROJECT_MEMORY.md` 中已经确认的内容，也不要重复询问已经确认的问题。能从代码、配置、Git 历史、需求文件或部署文件确认的信息，请自行检查。只有缺失信息会直接改变实现结果或产生数据风险时，才一次询问我一个最关键的问题。

开始任何任务前，请依次完成：

1. 完整阅读 `CODEX_PROJECT_MEMORY.md`；
2. 阅读仓库根目录及适用子目录中的 `AGENTS.md`；
3. 按 `AGENTS.md` 读取 `agent-memory/INDEX.md`，只打开与当前任务有关的主题记忆；
4. 检查当前 Git 分支、HEAD、工作区、远端分支和相关 Pull Request 状态；
5. 核对交接文件、需求文档和仓库当前实际状态；
6. 阅读当前任务涉及的数据模型、服务、序列化器、视图、路由、前端调用链和测试；
7. 明确修改范围后再开始实施。

如果交接文件中的分支、Commit、PR、镜像版本、数据库迁移或线上状态已经变化，以当前代码、Git/GitHub 状态、服务器真实输出和我的最新明确指令为准。不要从旧 `main` 重新开发尚未合并的功能。

当前交接时的动态线索如下，仅用于定位，接手后必须重新核验：

- 仓库：`kevindongbo/codex`；
- 本地目录：`F:\codex-ozon`；
- 当前实施分支可能为 `codex/implement-erp-replenishment-intransit-store-20260811`；
- 当前 Draft PR 可能为 `https://github.com/kevindongbo/codex/pull/12`；
- 交接时 Head 为 `5b86a1192a2cabbfa564de9e842aa39d93bd87fa`；
- PR #12 仍有若干 P0/P1 项未完整闭环，不能因测试通过就描述成全部需求完成；
- 生产已确认的运行方式是 `/opt/dongbo/app` + `/opt/dongbo/venv` + PostgreSQL + Gunicorn/systemd `dongbo-erp` + Nginx；
- 仓库虽然包含 Docker Compose/Caddy 配置，但当前生产没有被确认使用 Docker 或阿里云 ACR；在真实配置确认前，不要猜 ACR 地址、镜像名、容器名或 Compose 文件。

修改代码时：

- 只修改与当前任务相关的文件；
- 保留用户已有的无关改动和未跟踪文件；
- 不使用破坏性 Git 或数据库操作处理工作区；
- 涉及库存、采购、订单、补货、OAuth 或利润公式时，先核对已确认的业务约束；
- 同时补充后端和前端自动化测试；
- 实际运行适用的 Django 检查、迁移检查、后端测试、Node 语法检查、前端测试和构建；
- 测试失败或需求未完成时如实记录，不得虚构通过；
- 测试通过后只暂存本次相关文件，实际 Commit 并 Push 到正确功能分支；
- 创建或更新可审查的 Draft PR，写明修改、迁移、测试、风险、回滚和完整 SHA；
- 未经我明确授权，不自动合并，不连接或部署生产。

完成后，请按以下内容汇报：完成结果、修改内容、修改文件、业务逻辑变化、数据库迁移、测试结果、构建和静态检查、GitHub Commit、GitHub 分支、Pull Request、阿里云更新命令、回滚方式和注意事项。没有数据库迁移时也要明确写“本次无数据库迁移”。

阿里云命令必须依据仓库和服务器真实配置生成，使用最终精确 Commit SHA，并整理成一整段可直接复制执行的 Bash。命令至少要包含部署前状态检查、代码和 PostgreSQL 备份、精确提交检出、依赖安装、迁移、前端构建、静态收集、服务重启、Nginx 检查、健康检查、日志检查和安全回滚点。提供命令不等于部署成功；只有收到实际服务器输出后才能确认部署和健康检查结果。

阅读完成后，不需要复述整份记忆。请直接检查当前实际状态，并等待或执行我接下来的具体任务。
