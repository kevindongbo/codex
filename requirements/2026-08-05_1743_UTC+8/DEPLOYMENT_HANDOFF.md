# 阿里云部署交接

- 需求编号：`ERP-PROFIT-UI-REQ-20260805-01`
- 当前阶段：需求和代码审计已固化，修正代码尚未完成。
- 基线分支：`codex/implement-profit-settlement-strategies-20260805`
- 基线 HEAD：`807f207b35a7ea0b5b0b7448627146af4a58f852`

## 当前部署文档风险

现有`docs/deployment/aliyun/ERP-PROFIT-REQ-20260805-01.md`绑定的已测试代码 SHA 为`f5baf64555c34df11c82ac604283016afc6d749a`。本次策略 UI、API 和测试修改完成后，该 SHA 将不再代表最终部署代码，不能直接继续用于生产部署。

## Codex 完成后必须提供

1. 实施分支：`codex/implement-profit-strategy-ui-fixes-20260805`；
2. Draft PR 编号和地址；
3. 最终精确提交 SHA；
4. 实际测试命令和结果；
5. 是否产生 migration；
6. 是否增加 Python 或 Node 依赖；
7. 更新后的`docs/deployment/aliyun/ERP-PROFIT-REQ-20260805-01.md`。

## 更新后的部署文档必须包含

- `/opt/dongbo/app`代码备份；
- PostgreSQL 数据库`dongbo_erp`备份；
- 精确 SHA 拉取和检出；
- Python 依赖安装；
- migration 检查和执行；
- 前端构建与 collectstatic；
- systemd 服务`dongbo-erp`重启和状态检查；
- nginx 配置检查和重载；
- `http://127.0.0.1:8000/api/health/`检查；
- 页面人工验收；
- 完整代码和数据库回滚命令。

## 本次新增人工验收项

1. 主配置区没有`计算币种`；右上角 MYR/CNY 切换仍可用。
2. 交易手续费调整位于第一行，BXP 与买家运费开关同一行。
3. 策略栏显示自定义名称；没有旧策略下拉框、页面内策略名称和地区输入。
4. 新建弹窗只填写名称和地区；买家运费关闭时地区置灰并保存西马。
5. 点击策略立即设默认，刷新页面仍加载最后点击策略。
6. 同名新建/另存覆盖成功，不产生重复记录。
7. 覆盖、另存、删除及删除二次确认可用。
8. 删除最后一个策略后恢复系统默认且不自动创建数据库策略。
9. 顶部利润卡为主币大字、辅助币小字并带`≈`。
10. 左侧费用明细只显示主币；右侧九项保持同行双币。
11. 多项费用默认折叠，重新计算后再次折叠。
12. 箭头为放大的粗线 SVG，与文字对齐；单项费用不出现箭头。

最终代码 SHA 未确定前，不要生成或执行新的固定 SHA 部署命令。
