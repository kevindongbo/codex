# Codex 调用提示词

- 生成时间：**2026-08-04 03:08（UTC+8）**
- 仓库：`kevindongbo/codex`
- 需求分支：`docs/codex-erp-requirements-20260804-0308`
- 需求文件：`requirements/2026-08-04_0308_UTC+8/ERP_REQUIREMENTS.md`

复制下面整段内容给 Codex：

---

你现在需要在 GitHub 仓库 `kevindongbo/codex` 中实施一组已经确认的 ERP 需求。不要只做分析或输出建议，必须实际修改代码、运行测试、提交并推送到 GitHub。

## 一、分支与文件

1. 获取远程最新分支。
2. 检出需求分支：
   `docs/codex-erp-requirements-20260804-0308`
3. 完整阅读：
   `requirements/2026-08-04_0308_UTC+8/README.md`
   `requirements/2026-08-04_0308_UTC+8/ERP_REQUIREMENTS.md`
4. 检查仓库根目录及相关子目录是否存在 `AGENTS.md`；如存在，先读取并遵守。
5. 如果存在 `agent-memory/INDEX.md`，只读取与 ERP、利润试算、TikTok 马来西亚、仓库和部署相关的主题，不要读取无关内容。
6. 从需求分支创建新的实施分支：
   `codex/implement-erp-requirements-20260804`
7. 不要直接修改 `main`，不要直接修改现有 PR #3 的分支指针，不要自动合并。

## 二、实施要求

严格按 `ERP_REQUIREMENTS.md` 实施全部纳入范围，包括：

- 删除智能补货两段说明文案，但不修改补货算法；
- 利润明细只有一项时显示单行，两项及以上才显示合计和默认展开明细；
- 汇率增加主来源和至少两个备用来源、数据库快照、定时刷新、全部在线来源失败时回退最近有效快照；
- 把直接竞品和间接竞品从商品中心迁移到竞品监控；
- 竞品以商品链接为核心，手动维护，不建立可见的独立竞品店铺模块；
- 相同竞品店铺名称默认联动店铺评分、明星店铺和卖家类型，发货类型不联动；
- 增加自有店铺管理；
- 一个 SKU 可关联多个自有店铺；
- 增加店铺售价、商品链接、专属佣金和“参与列表计算”开关；
- 商品列表保持一个 SKU 一行，显示售价、广告费前毛利、毛利率、保本 ROI、可用库存；
- 商品列表利润必须复用现有利润试算同一套后端规则，不得新写一套前端公式；
- 点击售价或利润结果时在当前行下展开店铺明细；
- “测算”进入多店铺对比测算页；
- 缺少必要资料时显示“待完善”，不得显示为 0；
- 不改变表格行高，放大商品缩略图，使用 `object-fit: contain`；
- 增加数据库迁移、API、权限、审计日志和自动化测试。

明确不要开发：

- 数据中心“今天 / 本月 / 全部”统计；
- 仓库出库销量、销售额和成本看板；
- 独立可见的竞品店铺管理模块；
- 自动抓取或伪造 TikTok 公开商品链接数据。

不要修改无关页面、无关文案、无关业务规则。不要改变已确认的利润公式、补货算法、规则来源链接和商品编辑弹窗现有图片区域。

## 三、先审计再编码

编码前先检查：

- `main`、`codex/erp-req-20260803-integration` 和当前需求分支的差异；
- PR #3 已有实现，避免重复或回退现有功能；
- Django 模型、迁移、序列化器、ViewSet、URL、权限和测试；
- 前端 `index.html`、`app.js`、`team.js`、`profit-calculator.js`、`styles.css` 和构建脚本；
- 当前利润计算 API 和规则版本；
- 当前竞品模型与快照模型；
- 当前汇率缓存和错误处理；
- 当前阿里云部署文档。

发现简单技术细节时自行采用稳妥方案，不要逐项提问。只有出现会明显改变业务流程或利润计算口径的真实冲突时才暂停说明。

## 四、代码质量

- 使用现有项目结构和命名风格；
- 数据库写操作需要事务时必须使用事务；
- 保持空值和数值 0 的区别；
- 避免前后端重复计算；
- 汇率的 MYR/CNY 与 MYR/USD 必须同快照原子更新；
- 多实例定时任务必须防重复执行；
- 竞品联动和历史快照由后端执行，前端不得模拟批量同步；
- 删除受业务引用的数据时返回明确错误，不得静默破坏历史；
- 不要提交密钥、`.env`、数据库文件、日志或生产数据；
- 不要删除或弱化现有测试断言来让测试通过。

## 五、测试与验证

根据仓库实际结构运行全部适用检查，至少包括：

```bash
cd backend
python manage.py makemigrations --check --dry-run
python manage.py test apps.erp.tests
cd ..
node --check app.js
node --check team.js
node --check profit-calculator.js
node --test tests/site.test.mjs
node --test tests/domain.test.mjs
node --test tests/team.test.mjs
npm run build
```

如果实际命令或目录不同，以仓库现有配置为准，但必须在最终报告列出每条真实执行命令和结果。

新增测试必须覆盖：

- 利润明细单项和多项；
- 汇率三来源回退和数据库历史回退；
- 自有店铺唯一、停用和受限删除；
- SKU 多店铺和佣金空值/0；
- 商品列表多店铺范围和待完善；
- 竞品店铺字段联动、商品移动、快照去重；
- 商品图片不改变行高；
- 产品中心竞品入口已移除；
- 数据中心统计未被新增。

## 六、GitHub 提交

完成后：

1. 检查 `git diff`，确认没有无关修改。
2. 使用清晰的小提交或一组逻辑明确的提交。
3. 推送分支：
   `codex/implement-erp-requirements-20260804`
4. 创建 Draft PR：
   - Base：`codex/erp-req-20260803-integration`
   - Head：`codex/implement-erp-requirements-20260804`
   - 标题：`ERP-REQ-20260804: 店铺、商品利润、竞品与汇率完善`
5. PR 正文必须包含：
   - 已完成需求；
   - 暂缓范围；
   - 数据库迁移；
   - 测试命令和结果；
   - 已知风险；
   - 回滚注意事项；
   - 精确部署提交 SHA。
6. 不自动合并，不部署生产。

## 七、阿里云部署文件

代码和测试全部完成后，根据实际实施分支和最终提交 SHA，新建：

`docs/deployment/aliyun/ERP-REQ-20260804.md`

参考：

- `docs/deployment/aliyun/ERP-REQ-20260803-01.md`
- 仓库现有 `DEPLOYMENT_ERP_OPERATIONS.md` 或等价部署文档。

部署文档必须使用真实值，不得使用占位符：

- 分支：`codex/implement-erp-requirements-20260804`
- SHA：最终已测试提交 SHA
- 应用目录：以现有部署文档为准，当前已知为 `/opt/dongbo/app`
- 虚拟环境：当前已知为 `/opt/dongbo/venv`
- systemd 服务：当前已知为 `dongbo-erp`
- PostgreSQL 数据库：当前已知为 `dongbo_erp`

文档必须给出三段可复制到阿里云黑框执行的代码：

1. 正式部署；
2. 部署后验证；
3. 一键回滚。

正式部署必须：

- 检查工作区干净；
- 拉取并验证真实提交 SHA；
- 备份当前代码和 PostgreSQL；
- 固定检出精确 SHA，而不是只跟随分支最新提交；
- 安装依赖；
- 加载 `.env` 但不打印内容；
- 执行迁移和静态文件收集；
- 重启 `dongbo-erp`；
- 检查 Nginx；
- 调用健康检查。

回滚必须明确警告：恢复数据库备份会丢失部署后新增数据。

## 八、最终回复格式

完成后只给出有证据的结果：

1. 实施分支名称；
2. 最终提交 SHA；
3. Draft PR 链接；
4. 修改文件摘要；
5. 数据库迁移摘要；
6. 每条测试命令及通过/失败结果；
7. 未完成项目及原因；
8. 阿里云部署文档路径；
9. 将部署、验证、回滚三段黑框命令完整贴出。

任何测试失败或需求未完成都必须如实标明，不能写成已经完成。

---
