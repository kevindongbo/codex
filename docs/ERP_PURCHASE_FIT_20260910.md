# 采购列表整页宽度适配

基线 7412f06a90c548c8b44beeed39ccc93ae9333803；实现分支 codex/erp-usability-audit-20260906。

- 物流编号统一 13px/600 字重，历史编号脱离 small 标签；历史来源标签仍独立保留，不改数据分类。
- 商品摘要取消三角箭头及其占位，单/多 SKU 图片、文字起点一致；保留键盘可操作的原生 details/summary。
- 十列按百分比分配，取消 1590px 最小宽度。长编号允许换行，操作纵向排列，不隐藏任何字段或按钮。
- 保留收起隐藏数量、单 SKU 不重复数量、国际单号分组等行为。
- 资源版本 20260910-purchase-fit-3。

验证：构建、app.js 语法、前端 63/63；真实 Edge 采购页面 1280/1440/1920 无横向溢出、表头宽度与位置一致、图片起点一致、无箭头、编号字号至少 13px；页面和控制台错误 0。采购保存、物流补录、主力仓调用回归；另运行 erp-usability（18组合）、replenishment-sticky（两尺寸）通过。

本轮仅前端渲染/CSS及测试修改，没有后端、迁移、依赖、环境变量、定时任务变化；后端完整测试本轮未重跑（前轮175/175不计为本轮结果）。生产、公网缓存及登录后真实数据：未验证。

沿用现有发布脚本，构建前端并核验源站文件；不需要 collectstatic。0037 若之前未应用仍需原发布流程完成，不能 fake。回滚到备份 previous-sha.txt 的代码，保留数据库和物流数据。
