# Codex 执行提示词

> 对应需求编号：ERP-REQ-20260803-01  
> 提示词上传时间：2026-08-03 03:33（北京时间，UTC+8）  
> 需求文件：`docs/requirements/2026-08-03-erp-profit-warehouse-optimization/README.md`

将下面整段内容复制给 Codex 使用。

---

你正在维护 GitHub 仓库 `kevindongbo/codex`。

请完整阅读并严格执行以下需求文件：

`docs/requirements/2026-08-03-erp-profit-warehouse-optimization/README.md`

需求编号：`ERP-REQ-20260803-01`。

## 一、执行前必须先做代码审计

不要立刻开始盲目修改。先检查并比较：

- `main`
- `codex/profit-calculator-server`
- `fix/profit-fee-rules-20260731`
- 当前线上实际部署分支
- 其他包含仓配中心、仓间调拨、智能补货、认证会话最新实现的分支

针对每个功能选择最新、最完整的实现作为基线，不要把所有修改强行堆到一个旧分支，也不要用旧代码覆盖新功能。

先输出一份“实现审计表”，至少包含：

1. 需求项。
2. 当前实现所在文件和函数。
3. 当前状态：已完成、部分完成、未完成、线上不可用、需回归。
4. 计划使用的基线分支。
5. 是否涉及数据库迁移、后端接口、前端、定时任务或权限。
6. 风险与依赖。

特别注意：PR #1 和 PR #2 都未合并，且存在利润、平台费用和运费口径差异。最终实现必须以需求文件和当前有效 TikTok Shop 马来西亚官方规则为准，不允许同时保留互相冲突的两套计算逻辑。

## 二、分模块创建修复分支

不要固定使用同一个开发分支。按模块从其最新完整实现创建修复分支，例如：

- 利润试算与规则管理
- 汇率更新
- 仓间调拨
- 智能补货与仓配 UI
- 登录刷新会话
- 全局交互与箭头样式

分支名必须清晰表达模块与日期。记录每个修复分支的来源分支和来源 commit。

## 三、实现顺序

建议按以下顺序执行：

1. 统一利润试算计算规则和测试基线。
2. 完成规则版本、权限、定时生效与回滚。
3. 完成汇率自动更新、手动刷新和手动修正。
4. 完成仓间调拨状态机、库存事务、部分收货和撤回。
5. 修复智能补货全选、重新计算和权重覆盖。
6. 修复登录刷新恢复流程。
7. 完成采购折叠、字体、箭头和费用明细交互。
8. 做跨分支集成和浏览器回归。

## 四、必须遵守的业务规则

### 利润试算

- 删除利润计算页全部黄色说明框，统一迁移到“规则与说明”。
- 费用顺序：商品售价、平台代扣合计、商品采购成本、达人佣金、税费、物流费用、广告费用。
- 零金额项目仍显示。
- 平台代扣合计默认收起，整行和箭头都能展开/收起。
- 展开后只显示明细，不在底部重复显示小计或合计。
- 类目佣金调试显示为“类目佣金调试（按百分点临时调整）”，仅当前试算生效。

### TikTok Shop 马来西亚费用

- 交易手续费按当前官方规则：`(商品原价 - 商家折扣 + 买家支付运费 - 顾客订单退款金额) × 3.78%`。
- 不存在 RM4.13 封顶。
- 买家运费进入交易费基数，但不进入商品售价。
- 平台支持费按当前有效规则版本计算；默认 RM0.54/成功送达订单，支持豁免规则。
- 多 SKU 平台支持费按折后销售金额占比分摊，尾差给折后金额最高的 SKU。
- 类目佣金按类目、卖家类型、BXP、日期和有效版本读取。
- BXP 独立展示，不能与类目佣金合并。

### 规则管理

- 所有账号可查看，只有主账号持有者可修改、定时生效、取消计划和回滚。
- 权限必须后端校验。
- 修改原因必填，保存前二次确认。
- 支持立即生效和按北京时间所选日期 00:00 定时生效。
- 支持选择任意历史版本回滚；回滚生成新版本，不删除历史。

### 汇率

- 支持 CNY、MYR、USD 三币种相互兑换，使用市场中间价。
- 通过已配置且可联网搜索的 DeepSeek，每天北京时间 09:00 自动更新。
- 服务端必须校验来源、时间、正数、倒数关系、交叉汇率和异常波动。
- 所有登录账号可手动刷新。
- 只有主账号可手动修正；不需要原因和二次确认；手动值仅当天有效。
- 前端不提供汇率历史入口，但后台保留日志。

### 仓间调拨

- 状态：草稿、调拨在途、部分调入、已调入、已取消。
- 进行中只包括调拨在途和部分调入，不包括草稿。
- 草稿仅在全部中显示，可编辑和删除，不扣库存。
- 正式发出后调出仓固定；草稿和进行中可修改调入仓。
- 调拨在途可新增、删除商品并增减数量。
- 支持按 SKU 多次部分收货。
- 每次收货记录可单独撤回。
- 撤回时库存不足必须禁止，不能产生负库存。
- 部分收货后只可编辑剩余在途部分；已收货数量需先撤回才能改。
- 所有库存变化必须使用数据库事务、库存流水和审计记录。
- 所有发出、收货、撤回、取消接口必须幂等。

### 智能补货

- 默认权重：3 天 40%、7 天 30%、15 天 20%、30 天 10%。
- 总和不等于 100% 禁止保存。
- 全局修改仅覆盖当前仓库全部有效 SKU，并覆盖旧的单独权重；必须二次确认。
- 可勾选一个或多个 SKU 单独批量修改。
- 全选必须选择当前仓库全部有效 SKU，不受筛选和分页影响。
- 未选中 SKU 时重新计算全部；有选中时只计算所选。
- 删除“信心度”展示。
- 周期明细、补货参数和计算依据默认收起。

### 登录刷新

- 用户只要求同一标签页 F5 刷新不退出，不要求关闭浏览器后持续登录。
- 优先修复现有 `sessionStorage + refresh token` 恢复流程。
- 临时断网、500、超时不得无提示清空会话。
- 只有 refresh token 失效、账号停用、后端明确拒绝或主动退出时才返回登录页。

## 五、质量要求

1. 所有库存相关修改必须使用数据库事务。
2. 所有可能重复提交的业务操作必须幂等。
3. 前端隐藏按钮不能代替后端权限控制。
4. 不得出现重复扣库存、重复加库存或负库存。
5. 不要只修改静态页面而不接真实接口。
6. 检查静态资源版本和缓存，确保线上加载新代码。
7. 保留必要的审计字段：操作人、时间、修改前后数据、关联单号和原因。
8. 不要为了统一 UI 而把原本美观的箭头全部替换；只修复明显丑陋、不居中、尺寸不协调的箭头。

## 六、测试要求

新增或更新后端单元测试、API 测试、前端 DOM/交互测试和必要的浏览器回归测试。需求文件第 15 节列出的测试场景全部覆盖。

运行并记录仓库已有的全部相关测试命令。若现有测试失败，先说明是已有失败还是本次引入。

不得使用“代码里已经有按钮”作为完成依据。必须验证：

- 按钮可点击。
- 请求成功。
- 数据正确保存。
- 刷新后状态正确。
- 库存和流水正确。
- 权限正确。
- 错误路径有明确反馈。

## 七、交付格式

每个模块完成后，提交清晰的 commit。最终提交或 PR 描述必须包含：

1. 使用的基线分支和 commit。
2. 修改文件列表。
3. 数据库迁移说明。
4. 已完成需求清单。
5. 未完成或受限项。
6. 测试命令和结果。
7. 手工浏览器验收结果。
8. 已知风险。
9. 部署和缓存刷新步骤。
10. 回滚方案。

完成前请再次逐条对照 `README.md`，不得遗漏用户已经确认的细节，也不得自行改变业务规则。遇到真正无法从代码或需求判断的问题时，只提出一个最关键的问题，不要一次抛出大量问题。

---
