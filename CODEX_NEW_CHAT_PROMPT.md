# 东铂跨境 ERP 新对话启动提示词

我正在继续一个已经开发了一段时间的东铂跨境 ERP 项目。

我已经上传 `CODEX_PROJECT_MEMORY.md`。请先完整阅读该文件，并将其作为本项目的长期记忆、协作规则和上下文来源。不要让我重新解释文件中已经确认的内容。

默认使用中文，表达直接、清楚、可执行。请根据记忆文件恢复之前的工作习惯，包括代码修改前的检查流程、GitHub 仓库与分支规则、Commit 和 Draft Pull Request 流程、测试与构建要求，以及阿里云生产更新和回滚方式。

开始任何任务前，请严格按以下顺序执行：

1. 完整阅读 `CODEX_PROJECT_MEMORY.md`；
2. 阅读仓库根目录及适用子目录中的 `AGENTS.md`；
3. 按规则读取 `agent-memory/INDEX.md`，只打开与当前任务相关的主题记忆；
4. 执行并检查 `git status --short`、`git branch -vv`、`git log --oneline --decorate -20`、`git reflog -20`；
5. 检查当前 HEAD、远端跟踪、未推送代码和相关 Pull Request 状态；
6. 核对交接文件、需求文档与仓库当前实际状态；
7. 阅读当前任务涉及的模型、服务、序列化器、视图、路由、前端调用链和测试；
8. 明确修改范围后再开始实施。

如果记忆文件中的分支、Commit、PR、迁移、服务器版本或线上状态已经变化，以当前代码、Git/GitHub 状态、服务器真实输出和我的最新明确指令为准。不要从旧 `main` 重新开发尚未合并但已存在于功能分支上的功能。

能从代码、配置、Git 历史、需求文件或项目文档确认的信息，请自行检查；只有缺失信息会直接影响实现结果、业务口径或数据安全时，才一次问我一个最关键的问题。简单且有正常 ERP 行业做法的细节可以自行决定。

修改代码时：

- 只修改当前任务相关内容，不重写无关模块；
- 优先复用现有模型和调用链，不创建第二套重复实现；
- 不破坏已有数据、库存流水、锁库逻辑、利润公式、补货算法或线上功能；
- 涉及数据库变更时提供 migration、备份、验证和回滚方式；
- 不泄露或提交密码、Token、私钥、AccessKey、数据库密码或生产 `.env`；
- 不直接修改 `main`，不自动 merge，不直接部署生产；
- 实际运行适用的后端测试、前端测试、构建、静态检查和真实浏览器回归，不虚构结果；
- 只暂存本次相关文件，实际 Commit 并 Push 到正确功能分支，创建或更新可审查的 Draft Pull Request。

完成后请汇报：完成结果、修改内容、修改文件、业务逻辑变化、数据库迁移、测试结果、构建和静态检查、GitHub Commit、GitHub 分支、Pull Request、阿里云更新命令、回滚方式和注意事项。没有数据库迁移时也要明确写“本次无数据库迁移”。

阿里云更新命令必须依据仓库和服务器真实配置生成，使用最终精确 Commit SHA，并整理成一整段可直接复制到服务器黑色终端执行的 Bash。当前生产架构如仍为 PostgreSQL + Django/Gunicorn + systemd + Nginx，就不要擅自改用仓库里的 Docker Compose，也不要虚构 ACR 镜像信息。提供命令不等于部署成功；只有收到真实服务器输出后，才能确认部署和健康检查成功。

现在请先完成上述读取和核验，不需要复述全部记忆，然后等待我的具体任务。
